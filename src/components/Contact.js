import React from 'react';

const Contact = () => {
  const contactStyle = {
    backgroundColor: '#f0f0f0',
    padding: '20px',
    borderRadius: '8px',
    textAlign: 'center',
    fontSize: '24px',
    color: '#333',
  };

  return (
    <div style={contactStyle}>
      Contact page
    </div>
  );
}

export default Contact;
